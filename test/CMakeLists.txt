add_executable(strcaltest test.c)
add_dependencies(strcaltest strcaltest)
add_test(strcaltest strcaltest)
target_include_directories(strcaltest PRIVATE ${PROJECT_SOURCE_DIR}/src ${CMOCKA_INCLUDE_DIR})
target_link_libraries(strcaltest strcal ${CMOCKA_LIBRARY})